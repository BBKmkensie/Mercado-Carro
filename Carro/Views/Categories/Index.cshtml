@model IEnumerable<Carro.Models.Category>

@{
    ViewData["Title"] = "Index";
}

<h1>Category</h1>

<p>
    <a asp-action="Create"><i class="fa-solid fa-circle-plus"></i></a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Descripcion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion)
            </td>
            <td>
                    <a asp-action="Edit" asp-route-id="@item.CategoryId" class="text-danger"><i class="fa-solid fa-pen m-1"></i></a> |
                    <a asp-action="Details" asp-route-id="@item.CategoryId" class="text-danger"><i class="fa-regular fa-clipboard"></i></a> |
                    <a asp-action="Delete" asp-route-id="@item.CategoryId" class="text-danger" onclick="return confirm('¿Estas Seguro?')">
                        <i class="fa-solid fa-trash m-1"></i>
                    </a>
            </td>
        </tr>
}
    </tbody>
</table>
